{
  "enabled": {
    "description": "启用插件",
    "type": "bool",
    "hint": "关闭后不做 provider 路由，也不记录 pending reservation。",
    "default": true
  },
  "timezone": {
    "description": "统计时区",
    "type": "string",
    "hint": "用于计算每日重置窗口，例如 Asia/Shanghai。",
    "default": "Asia/Shanghai"
  },
  "reset_time": {
    "description": "每日额度重置时间",
    "type": "string",
    "hint": "格式 HH:MM。当前火山资源包业务按北京时间 11:00 切换日窗口。",
    "default": "11:00"
  },
  "default_daily_limit_tokens": {
    "description": "默认每日额度",
    "type": "int",
    "hint": "仅作用于 volcengine_provider_source_ids 指定的火山模型。单位 token。",
    "default": 2000000
  },
  "default_safety_buffer_tokens": {
    "description": "安全余量",
    "type": "int",
    "hint": "仅火山模型使用；判断是否可用时会预留这些 token，降低实际越线概率。",
    "default": 100000
  },
  "default_request_reservation_tokens": {
    "description": "单请求预占 token",
    "type": "int",
    "hint": "仅火山模型使用；请求发出前无法知道真实 usage，因此先按该值预占。",
    "default": 50000
  },
  "reservation_ttl_seconds": {
    "description": "预占超时时间",
    "type": "int",
    "hint": "异常请求未完成时，pending reservation 最多保留多少秒。",
    "default": 1800
  },
  "overlay_ttl_seconds": {
    "description": "响应后临时叠加时间",
    "type": "int",
    "hint": "响应 usage 会先临时叠加一段时间，等待 AstrBot ProviderStat 异步落库。",
    "default": 180
  },
  "count_cached_input_tokens": {
    "description": "统计缓存输入 token",
    "type": "bool",
    "hint": "开启后按 input_other + input_cached + output 统计，更保守。",
    "default": true
  },
  "quota_key_mode": {
    "description": "额度统计维度",
    "type": "string",
    "hint": "provider_model 更符合单一模型额度；provider_id 适合按供应商配置隔离。",
    "default": "provider_model",
    "options": ["provider_model", "provider_id"],
    "option_labels": ["按模型名", "按 Provider ID"]
  },
  "exhausted_action": {
    "description": "链路耗尽后的行为",
    "type": "string",
    "hint": "stop 会阻断 LLM；allow_paid 继续原 provider；use_last 使用链路最后一个 provider。",
    "default": "stop",
    "options": ["stop", "allow_paid", "use_last"],
    "option_labels": ["阻断请求", "允许付费风险", "使用最后一级"]
  },
  "dry_run": {
    "description": "演练模式",
    "type": "bool",
    "hint": "开启后只记录决策，不实际切换 provider 或预占额度。",
    "default": false
  },
  "use_astrbot_fallback_chain": {
    "description": "使用 AstrBot 默认 fallback 链",
    "type": "bool",
    "hint": "chains_json 为空时，自动使用 default_provider_id + fallback_chat_models。",
    "default": true
  },
  "fallback_watch_interval_seconds": {
    "description": "AstrBot fallback 检查间隔",
    "type": "int",
    "hint": "每次 LLM 请求前检查文件签名并即时更新；这里配置无请求时的后台兜底检查间隔，默认 5 分钟。",
    "default": 300
  },
  "strict_priority_order": {
    "description": "严格按链首优先",
    "type": "bool",
    "hint": "每次都从 fallback 列表第一项开始检查额度与模态，不沿用会话当前模型作为扫描起点。",
    "default": true
  },
  "disable_astrbot_error_fallback": {
    "description": "接管 AstrBot 错误 fallback（兼容键）",
    "type": "bool",
    "hint": "开启后，AstrBot 当前请求只使用本插件按实时链、额度、冷却、熔断和模态过滤后的安全 fallback；不再清空全部后续模型。",
    "default": true
  },
  "quota_cooldown_seconds": {
    "description": "达到阈值后的冷却秒数",
    "type": "int",
    "hint": "火山模型达到额度阈值后持续跳过的时间。默认 86400 秒；即使 11:00 已进入新窗口，也要等冷却结束。",
    "default": 86400
  },
  "unlimited_provider_prefixes": {
    "description": "不限额 Provider ID 前缀",
    "type": "list",
    "hint": "兼容配置。额度上限只由 volcengine_provider_source_ids 决定，所有非火山 Provider 均不按 token 限制。",
    "default": ["deepseek/"]
  },
  "upstream_quota_provider_prefixes": {
    "description": "按上游额度错误冷却的 Provider 前缀",
    "type": "list",
    "hint": "匹配这些前缀的模型不套用火山 token 阈值；返回 FreeUsageLimitError 后冷却该模型，并按独立探测周期确认上游额度恢复。当前 opencode 使用 opencode-zen/。",
    "default": ["opencode-zen/"]
  },
  "volcengine_403_circuit_enabled": {
    "description": "启用火山 403 组级熔断",
    "type": "bool",
    "hint": "火山开发者计划明确返回 AccountOverdueError 等账号级故障后，临时跳过同一 provider source 下的全部火山模型；普通请求级 403 不连坐。",
    "default": true
  },
  "volcengine_provider_source_ids": {
    "description": "火山 Provider Source ID",
    "type": "list",
    "hint": "用于识别同一火山账号下的模型组。本机火山模型当前使用 provider_source_id=openai。",
    "default": ["openai"]
  },
  "volcengine_403_cooldown_seconds": {
    "description": "火山 403 熔断秒数",
    "type": "int",
    "hint": "403 后整组火山模型的冷却时间，默认 1800 秒（30 分钟）。",
    "default": 1800
  },
  "volcengine_probe_check_interval_seconds": {
    "description": "火山恢复探测检查间隔",
    "type": "int",
    "hint": "后台多久检查一次是否到达半开探测时间，默认 30 秒。",
    "default": 30
  },
  "volcengine_probe_timeout_seconds": {
    "description": "火山恢复探测超时",
    "type": "int",
    "hint": "随机火山模型最小探测请求的最大等待时间，默认 30 秒。",
    "default": 30
  },
  "provider_error_cooldown_enabled": {
    "description": "启用单模型错误冷却",
    "type": "bool",
    "hint": "Provider 瞬态或未知边界故障只冷却实际失败的完整 Provider/模型配置；请求级错误不写健康冷却。",
    "default": true
  },
  "provider_error_cooldown_seconds": {
    "description": "单模型错误冷却秒数",
    "type": "int",
    "hint": "超时、连接失败、429、5xx 等明确 Provider 瞬态故障的独立冷却时间，默认 1800 秒（30 分钟）。",
    "default": 1800
  },
  "unknown_provider_error_cooldown_seconds": {
    "description": "未知 Provider 错误短冷却秒数",
    "type": "int",
    "hint": "无法明确分类、但看起来来自 Provider 的异常先短冷却，默认 300 秒；请求级 400/422、上下文和模态错误不会开启健康冷却。",
    "default": 300
  },
  "provider_error_request_max_retries": {
    "description": "单模型请求最大尝试次数",
    "type": "int",
    "hint": "本插件接管的 Agent 请求和 OpenAI-compatible 直连调用中，每个模型最多尝试几次。默认 1，失败后立即冷却并切换后续模型。",
    "default": 1
  },
  "provider_error_attempt_timeout_seconds": {
    "description": "单模型首响应超时秒数",
    "type": "int",
    "hint": "OpenAI-compatible 模型在该时间内没有返回首个响应就立即失败、冷却并切换。默认 20 秒；设为 0 关闭插件超时。",
    "default": 20
  },
  "upstream_quota_probe_initial_delay_seconds": {
    "description": "未知重置额度首次探测延迟",
    "type": "int",
    "hint": "opencode free 等未知刷新时刻的上游额度报错后，首次恢复探测前等待多少秒。默认 3600。",
    "default": 3600
  },
  "upstream_quota_probe_interval_seconds": {
    "description": "未知重置额度探测间隔",
    "type": "int",
    "hint": "额度尚未恢复时，后续最小探测间隔。默认 3600 秒。",
    "default": 3600
  },
  "upstream_quota_probe_timeout_seconds": {
    "description": "未知重置额度探测超时",
    "type": "int",
    "hint": "每次最小恢复探测最长等待时间。默认 20 秒。",
    "default": 20
  },
  "provider_error_admin_notify_enabled": {
    "description": "Provider 错误私聊管理员",
    "type": "bool",
    "hint": "本插件接管的模型最终返回 Provider 错误时，私聊通知管理员，不在原会话展示错误文本。",
    "default": true
  },
  "provider_error_admin_notify_interval_seconds": {
    "description": "管理员错误告警间隔",
    "type": "int",
    "hint": "所有 Provider 错误共用一个持久化限频窗口，默认 3600 秒内最多私聊一次。",
    "default": 3600
  },
  "provider_error_suppress_current_chat": {
    "description": "原会话静默 Provider 错误",
    "type": "bool",
    "hint": "开启后，已转发给管理员的 Provider 错误不会在触发它的群聊或私聊窗口发送。",
    "default": true
  },
  "chains_json": {
    "description": "自定义路由链 JSON",
    "type": "text",
    "hint": "留空时使用 AstrBot 默认链。格式为数组：[{\"name\":\"doubao\",\"providers\":[\"provider-a\",\"provider-b\"],\"daily_limit_tokens\":2000000}]。",
    "default": ""
  },
  "allow_status_for_all": {
    "description": "允许所有人查看状态",
    "type": "bool",
    "hint": "关闭后只有 admin_user_ids 可使用 /quota status。",
    "default": true
  },
  "admin_user_ids": {
    "description": "管理员用户 ID",
    "type": "list",
    "hint": "为空时管理命令沿用 AstrBot 核心管理员权限；填写后这些 ID 作为额外命令管理员和错误通知目标。",
    "default": []
  },
  "exhausted_message": {
    "description": "额度耗尽提示",
    "type": "text",
    "hint": "链路耗尽且 exhausted_action=stop 时发送。可用变量：{refresh_time}。",
    "default": "当前模型链路今日免费 token 额度已用尽，将在 {refresh_time} 后恢复。"
  }
}
